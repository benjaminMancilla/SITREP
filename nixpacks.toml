[phases.setup]
nixPkgs = ["python311", "pango", "cairo", "gdk-pixbuf", "gobject-introspection", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"